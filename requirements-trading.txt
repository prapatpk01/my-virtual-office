# Trading Bot dependencies
# Install: pip install -r requirements-trading.txt

# Async HTTP (Telegram API + Alpaca REST)
aiohttp>=3.9.0

# Crypto exchange connectivity (Binance, Bybit, OKX)
ccxt>=4.2.0

# Numerical calculations (indicators: EMA, RSI, MACD, etc.)
numpy>=1.26.0

# Anthropic SDK (no longer required — AISignalStrategy is now API-free)
# Uncomment only if you use another strategy that calls Anthropic directly:
# anthropic>=0.25.0

# Forex / Gold data feed (free, no API key needed)
yfinance>=0.2.40

# Optional: for loading .env file automatically
# python-dotenv>=1.0.0
