{
  "name": "@personal-finance/ibkr-portfolio",
  "version": "0.1.0",
  "description": "Interactive Brokers Client Portal integration for portfolio, positions, allocation, performance, and market data",
  "private": true,
  "type": "module",
  "openclaw": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
